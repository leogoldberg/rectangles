runtime: php
env: flex

runtime_config:
  document_root: public

# Ensure we skip ".env", which is only for local development
skip_files:
  - .env

env_variables:
  # Put production environment variables here.
  APP_LOG: errorlog
  APP_KEY: base64:oHAv7QM8oBdgo+gYMzX9LBKZOlibUyCsOGe0JUNDFo0=

DB_CONNECTION: mysql
DB_DATABASE: mysql
DB_USERNAME: USER
DB_PASSWORD: password
DB_SOCKET: /cloudsql/asn3-4-db
